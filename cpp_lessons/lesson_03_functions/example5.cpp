
#include <stdio.h>

int main() {
	printf("Hello, World!\n");
}

// to run this example, compile it with
// `g++ example5.cpp -o example4.exe` and run `example4` on Windows or
// `g++ example5.cpp -o example4` and run `./example4` on Mac/Linux
