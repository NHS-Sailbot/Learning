
void my_func(int my_parameter, float my_other_parameter) {
    // do stuff with 'my_parameter' and 'my_other_parameter'
}

int main() {
    my_func(7, 2.5);
}

// to run example2.cpp, compile it with
// `g++ example2.cpp -o example2.exe` and run `example2` on Windows or
// `g++ example2.cpp -o example2` and run `./example2` on Mac/Linux
